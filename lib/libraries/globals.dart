
library localizer.globals;

double lat;
double long;
var weatherResponse;
var forecastResponse;
///setting a default value
DateTime lastWeatherUpdateDateTime=DateTime.now().subtract( Duration(minutes:30));

